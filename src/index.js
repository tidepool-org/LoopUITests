const { LoopTest } = require('./loopTest');
const { target,
    screenName,
    unit,
    setting } = require('./properties');

module.exports = {
    LoopTest,
    target,
    screenName,
    unit,
    setting,
};
