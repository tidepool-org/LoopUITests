const { Test } = require('./test');
const { target, screenName, unit, setting, limits } = require('./properties');

module.exports = {
    Test,
    target,
    screenName,
    unit,
    setting,
    limits,
};
